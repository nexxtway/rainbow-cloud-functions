{
    "name": "@rainbow-cloud-functions/automation",
    "version": "0.3.0",
    "main": "dist/index",
    "types": "dist/index",
    "author": "Nexxtway Team",
    "license": "MIT",
    "files": [
        "dist"
    ],
    "scripts": {
        "build": "npm run clean && npm run compile",
        "clean": "rm -rf ./dist",
        "compile": "tsc -p tsconfig.json"
    },
    "dependencies": {
        "node-fetch": "^2.6.1"
    },
    "devDependencies": {
        "firebase-admin": "^9.4.2",
        "lerna": "^3.22.1",
        "typescript": "^4.1.3"
    },
    "publishConfig": {
        "access": "public"
    }
}
